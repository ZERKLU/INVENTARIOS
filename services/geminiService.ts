// Este servicio ha sido desactivado ya que se eliminaron las funcionalidades de IA.
// Archivo mantenido como referencia vacía o para futura implementación manual.

export const analyzeImage = async (): Promise<null> => {
  return null;
};

export const suggestDescription = async (): Promise<string> => {
  return "";
};
